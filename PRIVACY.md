# Tibo Watch 隐私与风险说明

## 本地处理

Tibo Watch 的规则分类、去重和调度均在本机完成。首版不调用外部 AI 模型，不提供账号系统或公网后端，也不会自动上传动态历史、邮箱设置或 X 会话。

## 网络请求

应用仅在相应功能开启时访问：

- GitHub 上的 Nitter 公共实例注册表；
- 可用的第三方 Nitter 实例及其 `@thsottiaux/with_replies/rss`；
- X/Twitter 登录与 `@thsottiaux` 页面；
- 用户自行配置的 SMTP 服务器。

第三方服务会按其自身政策看到常规网络信息，例如 IP 地址、User-Agent 和请求时间。

## 本地存储

应用在 `%APPDATA%\Tibo Watch\` 保存 SQLite 数据库和 Chromium 隔离会话。数据库包含动态正文、发布时间、原帖链接、分类证据、来源健康、通知状态、邮件重试队列和非敏感设置。

SMTP 应用密码经 Windows DPAPI 加密后写入数据库。加密与当前 Windows 用户绑定；复制到其他 Windows 账号通常无法解密。X 登录 Cookie 位于独立的 `persist:x-monitor` Chromium 分区，不与主界面共享。

## 删除与备份

卸载程序默认保留用户数据。彻底删除前请先从托盘退出应用，再删除 `%APPDATA%\Tibo Watch\`。备份或恢复也应在退出后复制整个目录，避免复制正在写入的 SQLite WAL 文件。

## X 自动化风险

X 登录抓取属于非官方页面读取，可能因页面结构变化、风控或服务条款而失败，并可能导致账号受限。该功能默认关闭。公共 RSS 同样依赖非官方第三方实例，无法保证实时性和可用性。用户应自行评估是否启用 X 登录抓取。

## 通知含义

“确认”表示规则识别到 Tibo 明确宣布重置或调整相关限制；它不是对个人账号额度状态的技术读取或保证。规则分类可能存在误报或漏报，重要操作应点击原帖复核。
