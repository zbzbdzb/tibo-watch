# Tibo Watch 隐私与风险说明

## 本地处理

Tibo Watch 的规则分类、去重和调度均在本机完成。首版不调用外部 AI 模型，不提供账号系统或公网后端，也不会自动上传动态历史、邮箱设置或 X 会话。

## 网络请求

应用仅在相应功能开启时访问：

- GitHub 上的 Nitter 公共实例注册表；
- 可用的第三方 Nitter 实例及其 `@thsottiaux/with_replies/rss`；
- Chrome 辅助扩展访问的 `@thsottiaux` 页面；
- Chrome 扩展与 Tibo Watch 之间的 `127.0.0.1:47652` 本机回环连接；
- 用户自行配置的 SMTP 服务器。

第三方服务会按其自身政策看到常规网络信息，例如 IP 地址、User-Agent 和请求时间。

## 本地存储

应用在 `%APPDATA%\tibo-watch\` 保存 SQLite 数据库。数据库包含动态正文、发布时间、原帖链接、分类证据、来源健康、通知状态、邮件重试队列和非敏感设置。

SMTP 应用密码经 Windows DPAPI 加密后写入数据库。加密与当前 Windows 用户绑定；复制到其他 Windows 账号通常无法解密。X 登录状态始终由 Chrome 保存；辅助扩展和 Tibo Watch 不读取、复制、导出或存储 Cookie、密码、Local Storage、Session Storage 或会话令牌。

辅助扩展只发送页面可见的公开动态字段：动态 ID、`@thsottiaux`、正文、引用正文、发布时间、动态类型和公开原帖链接。本机桥接仅监听 `127.0.0.1`；动态提交必须通过固定扩展来源与标识校验。只读健康探测兼容 Chrome 不携带 `Origin` 的 GET 请求，仍要求指定扩展标识，不返回动态、设置或凭据，不对无来源探测授予 CORS 权限。此过滤用于限制网页请求，并非对本机程序的身份认证。

## 删除与备份

卸载程序默认保留用户数据。彻底删除前请先从托盘退出应用，再删除 `%APPDATA%\tibo-watch\`。备份或恢复也应在退出后复制整个目录，避免复制正在写入的 SQLite WAL 文件。

## X 自动化风险

Chrome 登录共享属于非官方页面读取，可能因页面结构变化、风控或服务条款而失败，并可能导致账号受限。公共 RSS 同样依赖非官方第三方实例，无法保证实时性和可用性。用户应自行评估是否启用 Chrome 登录共享。

## 通知含义

“确认”表示规则识别到 Tibo 明确宣布用量重置已完成或正在执行，普通额度调整不等于重置；它不是对个人账号额度状态的技术读取或保证。规则分类可能存在误报或漏报，重要操作应点击原帖复核。

发送记录保留当时的动态、判定和目标收件人快照，以支持审计与按收件人重试。SMTP 已接受仅表示邮件服务器受理，不代表进入收件箱。服务器受理后应用异常退出可能造成结果不确定；相同 Message-ID 可辅助识别重复邮件，但不是严格仅一次保证。
